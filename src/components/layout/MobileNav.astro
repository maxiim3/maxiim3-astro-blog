---
import { getLangFromUrl, useTranslations, getAlternateLocale, translatePath } from "../../i18n/utils";

const lang = getLangFromUrl(Astro.url);
const t = useTranslations(lang);
const currentPath = Astro.url.pathname;
const alternateLang = getAlternateLocale(lang);
const alternateUrl = translatePath(currentPath, alternateLang);

const navItems = [
  { href: `/${lang}`, label: t("nav.home"), icon: "home" },
  { href: `/${lang}/blog`, label: t("nav.blog"), icon: "file-text" },
  { href: `/${lang}/projects`, label: t("nav.projects"), icon: "folder" },
  { href: `/${lang}/cv`, label: t("nav.cv"), icon: "user" },
];

function isActive(href: string): boolean {
  const path = currentPath.replace(/\/$/, "");
  const h = href.replace(/\/$/, "");
  if (h === `/${lang}`) return path === h;
  return path.startsWith(h);
}
---

<nav class="fixed bottom-0 left-0 right-0 z-50 md:hidden bg-base-100 border-t border-base-300">
  <div class="flex justify-around py-2">
    {navItems.map((item) => {
      const active = isActive(item.href);
      return (
        <a href={item.href} class:list={["flex flex-col items-center gap-1 p-2", active ? "text-primary" : "text-base-content/60"]}>
          {item.icon === "home" && (
            <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 12l2-2m0 0l7-7 7 7M5 10v10a1 1 0 001 1h3m10-11l2 2m-2-2v10a1 1 0 01-1 1h-3m-6 0a1 1 0 001-1v-4a1 1 0 011-1h2a1 1 0 011 1v4a1 1 0 001 1m-6 0h6" />
            </svg>
          )}
          {item.icon === "file-text" && (
            <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12h6m-6 4h6m2 5H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z" />
            </svg>
          )}
          {item.icon === "folder" && (
            <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 7v10a2 2 0 002 2h14a2 2 0 002-2V9a2 2 0 00-2-2h-6l-2-2H5a2 2 0 00-2 2z" />
            </svg>
          )}
          {item.icon === "user" && (
            <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M16 7a4 4 0 11-8 0 4 4 0 018 0zM12 14a7 7 0 00-7 7h14a7 7 0 00-7-7z" />
            </svg>
          )}
          <span class="text-xs">{item.label}</span>
        </a>
      );
    })}
    <a href={alternateUrl} class="flex flex-col items-center gap-1 p-2 text-base-content/60">
      <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 12a9 9 0 01-9 9m9-9a9 9 0 00-9-9m9 9H3m9 9a9 9 0 01-9-9m9 9c1.657 0 3-4.03 3-9s-1.343-9-3-9m0 18c-1.657 0-3-4.03-3-9s1.343-9 3-9m-9 9a9 9 0 019-9" />
      </svg>
      <span class="text-xs">{alternateLang.toUpperCase()}</span>
    </a>
  </div>
</nav>

<style>
  a { color: inherit; text-decoration: none; }
</style>
